const MessageThread = () => {
  return (
    <div>
      <h2>{'MessageThread'}</h2>
      <p>{'Find me in ./web/src/components/MessageThread/MessageThread.tsx'}</p>
    </div>
  )
}

export default MessageThread
